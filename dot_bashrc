#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# Only use fish by default for login shells, not when bash is explicitly requested
if [[ -z "$BASH_EXECUTION_STRING" && "$0" != *bash ]]; then
    exec fish
fi
